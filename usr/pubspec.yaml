name: couldai_user_app
description: "A Flutter app for managing class cash."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.0.5

flutter:
  uses-material-design: true
